import styled from 'styled-components';
import tw from 'twin.macro';

export const SearchTags = styled.aside`
  ${tw`flex grow flex-col sm:(px-[40px] gap-[40px] mb-[60px]) md:pr-[10px] lg:pr-[40px] x:(pr-[110px] gap-0 mb-0)`}
`;
